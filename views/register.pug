extends header.pug

block content
    div(class="ui" style="text-align:center;")
        h1(class="bottomheader2" style="font-family: 'Noto Sans KR', sans-serif;") 회원가입

    input(type="hidden" value=feedback class="feedback1")
    form(class="ui form form1" style="font-size:1.3rem;" action="./register_process" method="post")
        div(class="field")
            label 유저이름
            input(type="text" name="name" placeholder="Username" required)
        div(class="field")
            label 아이디
            input(type="text" name="id" placeholder="ID" required)
        div(class="field")
            label 비밀번호
            input(type="password" name="pwd" placeholder="password" required)
        div(class="field")
            label 비밀번호 재입력
            input(type="password" name="pwd2" placeholder="password" required)
        button(class="ui button but" type="submit") register

    script. 
        const feedback = document.querySelector('.feedback1');
        if (feedback.value)
            alert(feedback.value);